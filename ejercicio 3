#include<string.h>
#include <stdio.h>
char a [8];

int main()
{   
    for(int i=0; i<8; i++)
        {printf("ingrese los 8 valores de A: ");
        scanf("%s", &a[i]);}
    for (int i=0; i<8;i++)
        {printf("en la posición %i la letra %c\n",i+1, a[i]);}
    
    return 0;
}
