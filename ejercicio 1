#include <stdio.h>
int q [10];
int resultado=0;

int main()
{   
    for(int i=0; i<10; i++)
        {printf("ingrese los 10 valores de de z");
        scanf("%i", &q[i]);
            if (q[i]!=0){
                printf("ERROR el unico valor valido para q es 0");
                break;
            }
        } 
        
    

    return 0;
}
